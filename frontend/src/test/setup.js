// Carregado automaticamente antes de cada arquivo de teste (ver vite.config.js,
// test.setupFiles). Adiciona os matchers do jest-dom (toBeInTheDocument,
// toHaveTextContent etc.) ao expect() do Vitest — sem isso, só os matchers
// genéricos (toBe, toEqual...) estariam disponíveis.
import '@testing-library/jest-dom';
